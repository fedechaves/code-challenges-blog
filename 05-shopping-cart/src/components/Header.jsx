import Filters from './Filters'

// eslint-disable-next-line react/prop-types
const Header = () => {
  return (
    <header>
      <h1>React Shop 🛒 </h1>
      <Filters />
    </header>
  )
}

export default Header
